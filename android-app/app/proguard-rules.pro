# Project-specific R8 rules for DropDroid.
#
# The Android Gradle Plugin, Compose, CameraX, ML Kit, and kotlinx.serialization
# dependencies provide their own consumer rules. Keep this file so release
# shrinking is explicit and future reflection-based rules have one home.
